<%= turbo_stream.update :game_stage, partial: "game_sessions/items/uses/use",
      locals: { game_session: @game_session, item: @item, hp_before: @hp_before } %>
<%= turbo_stream.replace :character_sheet, partial: "characters/sheet",
      locals: { character: @item.character } %>
