<%# locals: (game_session:, hint:) -%>
<div class="stalled">
  <p class="hint"><%= hint %></p>
  <%= button_to "Try again", game_session_narration_path(game_session), class: "btn btn--secondary" %>
</div>
