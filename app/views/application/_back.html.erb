<%# locals: (to:, title:) -%>
<header class="page-head">
  <%= link_to to, class: "page-head__back", aria: { label: "Back" } do %>
    <%= render "glyph", name: "back" %>
  <% end %>
  <h1><%= title %></h1>
</header>
