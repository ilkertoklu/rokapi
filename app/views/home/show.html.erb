<%= button_to session_path, method: :delete, form_class: "profile", class: "profile__badge",
      aria: { label: "Çıkış yap" }, data: { turbo_confirm: "Çıkış yapılsın mı?" } do %>
  <%= Current.user.initial %>
<% end %>

<%= render "brand" %>

<div class="stack">
  <% if @resumable_game_session %>
    <%= link_to game_session_path(@resumable_game_session), class: "resume" do %>
      <span class="resume__icon"><%= render "glyph", name: "bell" %></span>
      <span class="resume__body">
        <span class="resume__title">Maceraya devam et</span>
        <span class="resume__text"><%= resume_summary(@resumable_game_session) %></span>
      </span>
    <% end %>
  <% end %>

  <%= link_to "Yeni macera kur", new_game_session_path, class: "btn btn--primary" %>
  <button class="btn btn--secondary" disabled>Kod ile katıl</button>
</div>
